// The contact channel went in 4.5.1: plugs find sockets through the screen
// atlas and marker lights. The file stays so that updating by importing
// over an old install overwrites the old builder instead of leaving it
// referencing removed code.
