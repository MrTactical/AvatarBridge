// The native-contact diagnostic went with the native contact path.
// The file stays so that updating by importing over an old install
// overwrites the old tool instead of leaving it referencing removed
// code. Contacts convert through ChilloutVR pointers and triggers.
